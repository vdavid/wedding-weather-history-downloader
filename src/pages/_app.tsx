import '../../styles/globals.css'
import type { AppProps } from 'next/app'

function WeatherHistoryDownloaderApp({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />
}

export default WeatherHistoryDownloaderApp
